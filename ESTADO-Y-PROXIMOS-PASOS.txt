========================================================================
ESTADO DEL PROYECTO — Test de carga de productos (Playwright)
Última actualización: 2026-06-05
========================================================================

QUÉ HACE EL PROYECTO
--------------------
Automatización con Playwright que valida que un MISMO producto, cargado por
distintos métodos (manual, código de barra, asignación múltiple, plantilla),
dé el MISMO precio — en distintos tipos de documento (factura, presupuesto,
pedido, etc.) y aplicando configuraciones (descuentos, moneda, cotización,
lista de precios). Los casos se leen de un Google Sheet.

CÓMO SE CORRE
-------------
  node test-rapido.js "https://docs.google.com/spreadsheets/d/<ID>/edit#gid=<GID>"
- El Sheet debe estar compartido como "Cualquiera con el enlace - Lector".
- Pasar la URL con el #gid de la pestaña que tiene los casos (lo toma de la URL).
- Toda la salida queda guardada en  resultado-corrida.log  (un banner por caso).
- El .env (URL_BASE, ADMIN_USER, ADMIN_PASS) tiene que existir para loguear.


========================================================================
LO QUE SE HIZO HOY (2026-06-04)
========================================================================
- CONFIGURACIÓN ALÍCUOTA (IVA por ítem) — implementada y andando en FACTURA:
    * Campo real en el DOM: ListaProductoVenta[guid].TipoIVAId (un <select> con
      widget chosen). Opciones: 0% / 2,5% / 5% / 10,5% / 21% / 27% / Exento / No Grav.
    * En el Sheet se usa en Configuraciones: "alicuota: 27" (o "10,5", etc.).
    * Se setea por JS/jQuery (val -> chosen:updated -> change): el click del
      widget chosen en la grilla es flaky (abre el dropdown equivocado o no es
      clickeable), pero setear por jQuery pega siempre y recalcula el TotalIVA.
    * Cambiar la alícuota dispara un recálculo del servidor (AJAX) que vuelve unos
      segundos después y puede revertir la línea. Por eso el loop: aplica ->
      espera networkidle + 2,5s -> RE-verifica -> re-aplica si revirtió (hasta 6
      pasadas), hasta que la alícuota aguanta el roundtrip.
    * El precio se lee de TotalIVA, así que al cambiar a 27% el precio sube solo
      (ej. 15000 neto -> 19050 con IVA, vs 18150 con 21%). En presupuesto/pedido
      se avisa que ahí el precio se asume Total×1,21 (IVA fijo) y una alícuota
      distinta de 21% puede no reflejarse.
- VENTA_UNIFICADA: probado y andando (era el último documento sin verificar).
- NAVEGACIÓN robusta (paginas/documents.js): goto con waitUntil 'domcontentloaded'
  + timeout 60s + 1 reintento; el networkidle quedó en try/catch (15s) para que
  no tanque si la app hace polling. Esto resolvió los timeouts de /Venta/Crear
  cuando el dev está lento.
- NOTIFICACIÓN A DISCORD (utiles/discordNotifier.js): una por cada caso (test)
  vía webhook. Muestra: resultado (✅/❌), tiempo del test (ej. "2h"), cuenta,
  producto, documento, tipos de carga, configuraciones y precio antes/después.
    * Tipos de carga = SOLO los que realmente cargaron (los que no aplican/no
      andan en ese doc no salen). Configuraciones = SOLO las que se aplicaron
      (configApplier.aplicar() ahora devuelve las aplicadas).
    * La URL va en .env como DISCORD_WEBHOOK_URL (gitignored). Si no está, avisa
      y sigue sin romper. Quedó .env.example con la clave de ejemplo.
- ORDEN FIJO DE CONFIGS (configApplier.aplicar): se ignora el orden del Excel y
  se aplican en: lista_precios -> moneda -> cotizacion -> descuento_item ->
  alicuota -> descuento_global (las desconocidas al final). La alícuota va casi
  al final porque otras configs la resetean.
- COTIZACIÓN arreglada: el bug era la TILDE — en el Excel venía "cotización" y el
  switch buscaba "cotizacion" -> nunca se aplicaba (quedaba el dólar default). Se
  normalizan las CLAVES al leer el Sheet (sin tildes + minúsculas); los valores
  NO se tocan (ej. "Dólar"). Además aplicarCotizacion ahora es robusto: diagnostica
  los campos CotizacionDolar, setea por fill+type+JS/eventos y re-aplica en pasadas.
- VENTANA MAXIMIZADA: launch con --start-maximized + newContext({viewport:null}).


========================================================================
LO QUE SE HIZO (2026-06-03)
========================================================================
- PEDIDO: verificados asignación múltiple y plantilla (4/4 métodos OK, 18150).
- REMITO soportado (dentro de productLoader.js, ruteado por this.documento):
    * Manual y Asignación Múltiple OK. Asign. múltiple usa su propio modal
      (#ModalBuscarMultiProducto: poner código en #NombreProducto -> blur ->
      tildar el check -> Agregar).
    * Código de barra y Plantilla NO existen en remito -> se omiten a propósito.
    * Remito NO maneja IVA: precio = Total tal cual (sin x1.21). Se ajustó en
      las 3 lecturas de precio (carga, "después de configs" y configApplier).
- CONFIGURACIONES (configApplier.js):
    * descuento_item: re-aplica por pasadas hasta que los N ítems queden con el
      valor (la tabla se re-dibujaba y perdía el descuento -> precios distintos).
    * descuento_global: distingue por formato -> "5" = MONTO FIJO $ (#ValorDescuento),
      "%5"/"5%" = PORCENTAJE (#Descuento). Antes todo iba a #Descuento (mal el $).
    * lista_precios: id del widget cambia por documento; PEDIDO no tiene ese
      módulo -> se omite (y ya no cuelga 30s si el widget no está).
    * moneda / lista_precios / descuento_item ahora en try/catch (una config que
      falla ya no mata el caso entero).
- CORRIDA: login con reintento (si no aparece el campo Email) + cada caso corre
  en su propio contexto (sesión limpia, no arrastra el login del caso anterior).


========================================================================
LO QUE SE HIZO (2026-06-02)
========================================================================
- Lectura de precios CON IVA, agnóstica al documento:
    * Factura: lee el campo TotalIVA directo.
    * Presupuesto / Pedido: no guardan IVA por línea -> se calcula Total x 1.21
      (IVA asumido = 21%, constante en el código).
- Carga SIN IDs select2 fijos (#select2-chosen-N): ahora ubica el select2 de
  producto por su clase .productoId y detecta la fila nueva dinámicamente.
- Selectores de producto agnósticos al documento. Tablas reconocidas:
    ListaProductoVenta (factura), ListaProductoPresupuestoVenta (presupuesto),
    ProductosLista (pedido).  (excluye las de "productos libres")
- Configuraciones implementadas y andando:
    * descuento_item   (incluso negativo = recargo)
    * descuento_global (campo #Descuento; se verifica por totales del documento)
    * moneda           (#MonedaId)
    * cotizacion       (#CotizacionDolar visible, sólo con moneda extranjera)
    * lista_precios    (#ListaDePreciosVentaId)
- Multi-configuración en una celda (separadas por coma) + valores con coma
  decimal (ej. "descuento_item: 10,5") sin romper el parseo.
- CSV parseado respetando comillas (celdas con comas no se rompen).
- gid tomado desde la URL del Sheet (si no hay, exporta la primera hoja).
- Log de corrida a archivo (resultado-corrida.log) con banner por caso.
- Corrida RESILIENTE: si un método no carga, se OMITE y la corrida sigue
  (no se tanca ni hay que cerrar el navegador). Más rápida (slowMo 80).
- Validación de CuentaID (si no es numérico, corta con mensaje claro).
- SOPORTE DE PEDIDO: manual y código de barra funcionan (misma carga que el
  resto; sólo cambiaba el nombre de la tabla = ProductosLista).
- ProductLoader "document-aware": recibe el documento y rutea Pedido a métodos
  exclusivos donde hace falta (asignación múltiple y plantilla).


========================================================================
ESTADO ACTUAL POR DOCUMENTO / MÉTODO
========================================================================
                       Manual  CodBarra  AsignMult  Plantilla   Configs
  factura                OK      OK        OK         OK          6/6 OK (+ alícuota)
  presupuesto            OK      OK        OK         OK          OK (sin moneda)
  pedido                 OK      OK        OK         OK          OK (sin moneda)
  venta_unificada        OK      OK        OK         OK          OK (+ alícuota, sin moneda/cotiz.)
  remito                 OK      N/A       OK         N/A         (sin config)

  (OK = probado y andando; ? = sin probar; N/A = el documento no tiene ese método)

NOTA REMITO: sólo tiene Manual y Asignación Múltiple (no tiene código de barra
ni plantilla; se omiten a propósito). Remito NO maneja IVA: el precio es el Total
tal cual (15000), sin el ×1.21 que se aplica a presupuesto/pedido. La asignación
múltiple de remito usa su propio método (modal #ModalBuscarMultiProducto: poner
el código en #NombreProducto, blur, tildar el check, Agregar).

NOTA: La carga MANUAL es un poco "flaky" (a veces falla el 1er intento por
re-render del select2), pero tiene REINTENTO automático + la resiliencia la
cubre, así que no bloquea.


========================================================================
LO QUE SE HIZO HOY (2026-06-05)
========================================================================
- PLANTILLA: elegir la plantilla POR NOMBRE (el del Excel), no la primera de la
  lista. Antes hacía ArrowDown+Enter (siempre la 1ª). Ahora escribe el nombre en
  el buscador del chosen y elige la coincidencia, con fallback por jQuery. Helper
  seleccionarPlantillaEnChosen reutilizado en factura/venta y pedido. (commit faf0289)

- RANGO DE PRECIOS (escala de descuento por cantidad): implementado y VERIFICADO.
    * Es una CONFIG dentro de Configuraciones: "rango de precios: N" (alias
      rango_precios / rango_de_precios). N = cantidad de unidades por cada carga.
    * configApplier.aplicarRangoPrecios setea la cantidad N en CADA línea cargada
      (campo <prefijo>[guid].Cantidad) y deja que FIDEL recalcule el escalón.
      Re-aplica por pasadas (roundtrip del server). NO escribe el precio, sólo la
      cantidad: el precio lo baja Fidel solo (eso es lo que se testea).
    * Modelo confirmado: el escalón es POR RENGLÓN (cada línea mira su propia
      cantidad). Como las 4 cargas quedan con la MISMA cantidad, coinciden.
    * El descuento del escalón aparece en el PRECIO unitario (.Precio), no en
      Bonificación. Producto de prueba (00069): cant 6 -> 1800/u (factura),
      cant 5 -> 1995/u (sigue en el 1er tramo, correcto). (commit a309099)

- COMPARACIÓN AMPLIADA: el test ahora compara POR LÍNEA, entre todas las cargas,
  los 4 campos de la grilla: Precio (unitario) / Cantidad / Bonificación / Total.
  El caso falla si alguno no coincide. (test-rapido)

- LECTURA DE PRECIOS = LO QUE MUESTRA FIDEL (sin inventar IVA). Bug viejo: en
  presupuesto/pedido no hay .TotalIVA por línea y el código hacía Total x 1.21
  (IVA 21% fijo) -> daba números que no existen en Fidel (este producto es 5% y
  presupuesto/pedido ni muestran IVA). Encima estaba duplicado en 3 lugares que se
  contradecían (12069.75 vs 9975).
    * Nueva util utiles/lecturaPrecios.js (leerLineasProducto): lee cada línea con
      TotalIVA si existe (factura/venta_unificada) o el Total NETO si no
      (presupuesto/pedido/remito). SIN calcular IVA.
    * La usan productLoader.leerPrecioNuevo, configApplier.leerPrecios y
      test-rapido (preciosDespues y el detalle salen de la MISMA lectura).
    * El log muestra la fuente por línea: [TotalIVA] o [Total (neto)].
    * Verificado: presupuesto/pedido -> [Total (neto)] 9975; factura/venta_unif
      -> [TotalIVA]; remito -> neto. (commit 746c94e)

- DISCORD: "Precio x producto (unit.)" SÓLO si el caso usa rango de precios; sin
  configuraciones muestra un único campo "Precio"; con configs, "antes" en su
  línea y "x producto"+"después" juntos en la misma. (commit a309099)

- 3 FIXES DE ROBUSTEZ (commit 6c5c438) VERIFICADOS corriendo:
    * moneda + cotización: convierte a USD OK (1995 ARS / 2000 ~= 1 USD), coincide
      con Fidel a mano. (OJO: el "8,86" viejo era de OTRO producto, el de 15000.)
    * carga manual: reintenta pero NO duplica (cantidad de precios = métodos que
      cargaron).
    * notificación Discord honesta: dijo "EXITOSO (3 de 4 métodos)" cuando faltó
      uno (código de barra inexistente -> omitido).


========================================================================
PENDIENTE  <<< EMPEZAR ACÁ
========================================================================
1. REGLAS DE DESCUENTO (config que falta) — DISEÑO ACORDADO 2026-06-05, falta
   AVERIGUAR un par de cosas en Fidel antes de codear.
   - QUÉ ES: una regla de descuento se define en OTRO MÓDULO de Fidel (no en la
     factura). Tipos: por CLIENTE, por ZONA o por VENDEDOR. Se elige el tipo + a
     qué cliente/zona/vendedor aplica + el descuento.
   - CLAVE: el descuento NO se ve en la factura normal; aparece recién al
     SELECCIONAR el cliente/zona/vendedor que tiene la regla.
   - ENFOQUE ACORDADO: la regla queda PRE-CREADA a mano en Fidel; el test NO la
     crea (eso sería tocar config persistente del sistema, riesgoso). El test solo
     VERIFICA el efecto: hace la factura para ese cliente/zona/vendedor, carga el
     producto y confirma que el precio viene con el descuento esperado.
   - POR TIPO:
       * por CLIENTE: ya sale de la columna ClienteID (elegir un cliente con regla).
       * por ZONA: la zona suele ser propiedad del cliente -> también vía ClienteID
         (un cliente de esa zona).
       * por VENDEDOR: a CONFIRMAR si la factura tiene un selector de vendedor que
         el test deba elegir -> sería una columna nueva "Vendedor".
   - PENDIENTE DE AVERIGUAR (experimento a mano en Fidel):
       1) Factura a un cliente SIN regla -> cargar 00069 -> anotar precio (~1995).
       2) Factura a un cliente CON regla por cliente -> cargar 00069 -> mirar:
          ¿el precio bajó? ¿apareció algo en la columna Bonificación, o el descuento
          está metido en Precio/Total sin mostrar el %? ¿se aplicó solo al elegir el
          cliente o hubo que hacer algo más?
       Con eso se sabe QUÉ campo leer y CÓMO validar.
   - DISEÑO TENTATIVO (cuando se confirme lo anterior): empezar por "por cliente".
     Fila con Documento=factura, ClienteID = cliente con regla, + columna nueva
     "Descuento_Esperado" (% o precio del escalón). El test carga el producto y
     verifica que el precio/bonificación refleje ese esperado. Después extender a
     zona y vendedor.

2. CASOS LÍMITE / NEGATIVOS: revisar el set de ~35 casos (descuento 0/100/decimal/
   negativo, CuentaID inválido, producto inexistente, fila sin métodos, etc.).


========================================================================
DÓNDE QUEDAMOS  <<< RETOMAR ACÁ
========================================================================
Pedido quedó COMPLETO y VERIFICADO (4/4 métodos). La fila 1268 se corrió el
2026-06-03 y los 4 métodos (manual, código de barra, asignación múltiple y
plantilla) dieron el MISMO precio: 18150. El fix de disparar por JS los links
del dropdown ("Asignación Múltiple" y #btnAbrirModal/Plantillas) resolvió el
"element is not visible" que rompía esos dos métodos. Confirmado y commiteado.

Remito quedó CERRADO (2026-06-03): Manual y Asignación Múltiple dan 15000 (sin
IVA). Código de barra y plantilla no existen en remito → se omiten.

Venta_unificada quedó PROBADO (2026-06-04): los 4 métodos andan. Era el último
documento sin verificar → ya están TODOS los documentos cubiertos.

Alícuota (IVA por ítem) quedó IMPLEMENTADA en factura (2026-06-04): campo
TipoIVAId, se setea por jQuery y se re-aplica hasta que aguanta el recálculo del
servidor. "alicuota: 27" sube el precio a 19050 (vs 18150 con 21%).

Rango de precios quedó IMPLEMENTADO y VERIFICADO (2026-06-05): config
"rango de precios: N", setea la cantidad por línea y Fidel recalcula el escalón.
También se arregló la lectura de precios (lee lo que muestra Fidel, sin inventar
IVA) y se verificaron los 3 fixes de robustez (moneda/cotización, manual sin
duplicar, noti "3 de 4").

>>> PRÓXIMO PASO: reglas de descuento (confirmar cómo se configura en Fidel) y/o
    casos límite/negativos. Todos los documentos y el resto de las configs ya
    están cubiertos.


========================================================================
CON QUÉ SEGUIR (para cubrir todo)
========================================================================
1. [HECHO ✓] Pedido: asignación múltiple + plantilla verificados (4/4 OK).

1b.[HECHO ✓] Remito: Manual + Asignación Múltiple OK (15000, sin IVA). Barra y
   plantilla no existen en remito → omitidos.

2. [HECHO ✓] Venta_unificada: los 4 métodos probados y andando (2026-06-04).

2b.[HECHO ✓] Alícuota (IVA por ítem) en factura: campo TipoIVAId, vía jQuery con
   re-aplicación hasta que aguanta el recálculo del servidor (2026-06-04).

2c.[HECHO ✓] Cotización: arreglada (era la tilde "cotización" vs "cotizacion").
   Claves del Excel normalizadas + aplicarCotizacion robusto. Da el mismo precio
   que a mano (2026-06-04).

2d.[HECHO ✓] Notificación a Discord por caso + orden fijo de configs + ventana
   maximizada (2026-06-04).

2e.[HECHO ✓] Alícuota en venta_unificada: probado y andando (2026-06-04). El
   código de alícuota es agnóstico al documento (busca el select TipoIVAId en
   las filas), así que funcionó igual que en factura sin tocar nada.
   NOTA: venta_unificada NO tiene moneda ni cotización -> ambas se omiten a
   propósito (skip explícito en configApplier, como lista_precios en pedido).

2f.[HECHO ✓] Plantilla por nombre (2026-06-05): elige la plantilla del Excel, no
   la primera de la lista. (commit faf0289)

2g.[HECHO ✓] Rango de precios (2026-06-05): config "rango de precios: N", setea la
   cantidad por línea y Fidel recalcula el escalón. Escalón POR RENGLÓN.
   Verificado en factura/presupuesto/pedido. (commit a309099)

2h.[HECHO ✓] Lectura de precios = lo que muestra Fidel, sin inventar IVA
   (2026-06-05): nueva util lecturaPrecios.js, saca el ×1.21 de presupuesto/pedido,
   unifica las 3 lecturas. (commit 746c94e)

2i.[HECHO ✓] 3 fixes de robustez (commit 6c5c438) VERIFICADOS corriendo
   (2026-06-05): moneda+cotización, manual sin duplicar, noti "3 de 4 métodos".

3. [PENDIENTE] Configuración que falta:
   - reglas de descuento (descuentos por cuenta / marca / categoría).
   (Se deja para cuando se confirme cómo funciona en el sistema.)

4. [MEJORADO ✓] Robustez (2026-06-04):
   - Carga manual: ya no duplica el producto si carga tarde (>8s). Antes de
     reintentar verifica si el intento anterior cargó y, si sí, no agrega otro.
   - Notificación honesta: si el test pasa pero faltó algún método, lo aclara
     ("✅ EXITOSO (3 de 4 métodos)").
   - moneda: ahora VERIFICA que el combo #MonedaId quedó en la moneda pedida
     (match sin tildes/mayúsculas) con fallback jQuery + reintentos, igual que
     cotización. Antes daba "aplicada" sin confirmar.

5. [VALORES] Revisar casos límite ya armados (descuento 0/100/decimal/negativo)
   y los negativos (CuentaID inválido, producto inexistente, sin métodos, etc.).
   El set completo de casos está en la tabla que se armó (35 casos).


========================================================================
NOTAS TÉCNICAS ÚTILES
========================================================================
- Archivos clave:
    test-rapido.js .............. orquestador (login -> cargar -> configs -> verificar)
    utiles/login.js ............. login admin + ID de cuenta (valida CuentaID)
    utiles/googleSheetsReader.js  baja el Sheet como CSV y parsea los casos
    paginas/documents.js ........ navega a cada tipo de documento
    componentes/productLoader.js  carga de productos (4 métodos + sección Pedido)
    componentes/configApplier.js  aplica las configuraciones (incl. rango_precios)
    utiles/lecturaPrecios.js .... lee las líneas con el valor que MUESTRA Fidel
                                  (compartida por los 3 lugares que leían precio)
- Detección de productos (regex, en 5 lugares):
    /^(?:ListaProducto(?!Libre)\w*?|ProductosLista)\[<guid>\]\.<campo>$/
- Precio reportado = lo que MUESTRA Fidel (utiles/lecturaPrecios.js): TotalIVA si
  existe (factura/venta_unificada); si no, Total NETO (presupuesto/pedido/remito).
  NO se calcula IVA (el viejo Total x 1.21 daba números que no existían en Fidel).
- Diagnósticos: patrón de escribir un debug-*.txt (ignorado por git) para
  inspeccionar el DOM cuando algo no se encuentra. Borrar al terminar.
- La carpeta de trabajo REAL es:
    D:\Users\Usuario\Documents\Playwright\Carga de productos repo\CargaDeProductos
  (hay una copia vieja en ...\Automatizaciones\... que NO se usa)
- Repo GitHub: https://github.com/VickyyGerb/CargaDeProductos
========================================================================
